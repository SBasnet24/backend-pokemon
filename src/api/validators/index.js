const trainerValidator = require('./trainer.validator');
const leagueValidator = require('./league.validator');
const pokemonValidator = require('./pokemon.validator');
const slotValidator = require('./slot.validator');

module.exports = {
  trainerValidator,
  leagueValidator,
  pokemonValidator,
  slotValidator,
};
